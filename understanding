wait, let's have a look. Apparently, they have been discussing APIs and this API. So on the cases of Java and good. And the hurry. Java and good, I designed basic, you know, the integration, like the jumps, everything is citizen girls. But here, this is not a simple CRUD app, I guess, because in the pathways, you have a com SATPRIC and many things. The things that I understand are like a repository, service, controller. Controller is the endpoint for all the like REST APIs. Then a repository handles all the database operations via GP and Hibernate. Service has the business logic, like validation kind of things. Then like the DTO, I don't see anything like specifically named as the DTO. And then config, I think config is for microservice to microservice communication. And DTO is also for like it takes data from the API and then stores it as an object. So DTO and configs are for microservice communication, I understand. And I think you should correct my understanding as well. So I have an understanding, but here I see util service, repository, properties, model, mapper, global exception, filter, factory, enumeration, entity, controller, config, annotation. Ah, entity also are the database features, like what do we store in database entities that I understand. And in React, I have understanding till components, props, how they are transferred, like the features from parent to child and Axios, useEffect, etc. the React basics. So this is my overall understanding now. On the basis of this, teach me how does CPR service and CPR UI like work together.
